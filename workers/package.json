{
  "name": "poli-worker",
  "version": "1.0.0",
  "description": "POLI Backend API with Cloudflare Workers and Hono",
  "main": "../backend/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.40.0"
  }
}
